{
  "name": "sse50-tracker",
  "version": "1.0.0",
  "description": "A real-time web application for tracking stocks from the Shanghai Stock Exchange 50 Index (SSE 50).",
  "main": "index.html",
  "scripts": {
    "start": "live-server --port=3000 --open=index.html",
    "dev": "live-server --port=3000 --open=index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["stock", "sse50", "china", "finance", "tracker"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
